"use strict";import{createClient}from"jsr:@supabase/supabase-js@2";import nodemailer from"npm:nodemailer@6.9.13";const DEEPSEEK_API_KEY=Deno.env.get("DEEPSEEK_API_KEY")||"sk-6733c8ac2b83402b8626e5e253824488";const DEEPSEEK_BASE_URL="https://api.deepseek.com";const SUPABASE_URL=Deno.env.get("SUPABASE_URL")||"https://fzcrjogrnujrfxafxbkh.supabase.co";const SUPABASE_SERVICE_ROLE_KEY=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")||"";const OPENROUTER_KEYS=[Deno.env.get("OPENROUTER_API_KEY"),"process.env.OPENROUTER_API_KEY","process.env.OPENROUTER_API_KEY","process.env.OPENROUTER_API_KEY","process.env.OPENROUTER_API_KEY","process.env.OPENROUTER_API_KEY","process.env.OPENROUTER_API_KEY"].filter(Boolean);Deno.serve(async req=>{try{const supabaseAdmin=createClient(SUPABASE_URL,SUPABASE_SERVICE_ROLE_KEY);const{data:deepseekOption}=await supabaseAdmin.from("api_keys").select("key_value").eq("service","disable_deepseek").maybeSingle();const disableDeepseek=deepseekOption?.key_value==="true";console.log("Checking engine status...");const{data:engineStatus}=await supabaseAdmin.from("agent_memory").select("value").eq("key_name","factory_status").maybeSingle();if(engineStatus?.value?.status==="paused"){console.log("Engine is PAUSED. Standing by.");return new Response(JSON.stringify({message:"Engine paused"}),{headers:{"Content-Type":"application/json"}})}const ukTimeOptions={timeZone:"Europe/London",hour12:false,hour:"2-digit",minute:"2-digit"};const ukTimeStr=new Intl.DateTimeFormat("en-GB",ukTimeOptions).format(new Date);const[ukHourStr,ukMinStr]=ukTimeStr.split(":");const ukHour=parseInt(ukHourStr,10);const ukMin=parseInt(ukMinStr,10);const ukMinutes=ukHour*60+ukMin;const startMinutes=7*60+14;const endMinutes=8*60+59;if(ukMinutes<startMinutes||ukMinutes>endMinutes){console.log(`Current UK Time is ${ukTimeStr}. Outside sending window (07:14 - 08:59). Standing by.`);return new Response(JSON.stringify({message:`Outside sending window (UK Time: ${ukTimeStr})`}),{headers:{"Content-Type":"application/json"}})}console.log("Checking for scheduled campaigns...");const{data:activeSchedules,error}=await supabaseAdmin.from("scheduled_emails").select(`
            *,
            campaigns!scheduled_emails_campaign_id_fkey!inner (
                id, name, status, company_name, contact_number, primary_email, business_id, current_step,
                businesses!inner (
                    id, name, status, signature_template
                )
            ),
            templates!scheduled_emails_template_id_fkey!inner (*)
        `).eq("status","scheduled").eq("campaigns.status","in_progress").eq("campaigns.businesses.status","active");if(error){console.error("Database query error:",error);throw error}if(!activeSchedules||activeSchedules.length===0){return new Response(JSON.stringify({message:"No active schedules"}),{headers:{"Content-Type":"application/json"}})}const filteredSchedules=(activeSchedules||[]).filter(schedule=>{const campaign=schedule.campaigns;const business=campaign?.businesses;if(!business||business.status!=="active"){console.log(`Skipping schedule ${schedule.id} because business status is not active (status: ${business?.status})`);return false}if(campaign?.business_id==="0269fe06-4607-4c58-9263-12a3930a1dc3"||business?.name&&business.name.toLowerCase().includes("mrmedic")||campaign?.name&&campaign.name.toLowerCase().includes("mrmedic")){console.log(`Skipping schedule ${schedule.id} because MrMedic campaigns are disabled.`);return false}return true});if(filteredSchedules.length===0){return new Response(JSON.stringify({message:"No active schedules (all filtered or inactive)"}),{headers:{"Content-Type":"application/json"}})}const results=[];const campaignGroups=new Map;for(const schedule of filteredSchedules){const cid=schedule.campaign_id;if(!campaignGroups.has(cid))campaignGroups.set(cid,[]);campaignGroups.get(cid).push(schedule)}for(const[_,schedules]of campaignGroups){schedules.sort((a,b)=>new Date(a.start_date).getTime()-new Date(b.start_date).getTime())}for(const schedule of filteredSchedules){const now=new Date;const endDate=new Date(schedule.end_date);const scheduledFor=new Date(schedule.scheduled_for);if(now>endDate)continue;if(now<scheduledFor){console.log(`Skipping schedule ${schedule.id}: Not due until ${scheduledFor.toISOString()}`);continue}const campaignSchedules=campaignGroups.get(schedule.campaign_id)||[];const stepIndex=campaignSchedules.findIndex(s=>s.id===schedule.id);if(stepIndex>0){const prevSchedule=campaignSchedules[stepIndex-1];const{data:pendingForPrev,error:prevErr}=await supabaseAdmin.rpc("get_pending_campaign_leads",{campaign_id_param:schedule.campaign_id,schedule_id_param:prevSchedule.id}).limit(1);if(prevErr){console.error("Error checking dependency",prevErr);continue}if(pendingForPrev&&pendingForPrev.length>0){console.log(`Skipping step ${stepIndex+1} (schedule ${schedule.id}): Previous step is not yet complete.`);const gapMs=24*60*60*1e3;let minStartMs=new Date(prevSchedule.start_date).getTime()+gapMs;if(minStartMs<now.getTime()+gapMs){minStartMs=now.getTime()+gapMs}if(new Date(schedule.start_date).getTime()<minStartMs){const newStart=new Date(minStartMs);const durationMs=new Date(schedule.end_date).getTime()-new Date(schedule.start_date).getTime();const newEnd=new Date(minStartMs+durationMs);await supabaseAdmin.from("scheduled_emails").update({start_date:newStart.toISOString(),scheduled_for:newStart.toISOString(),end_date:newEnd.toISOString()}).eq("id",schedule.id);schedule.start_date=newStart.toISOString();schedule.scheduled_for=newStart.toISOString();schedule.end_date=newEnd.toISOString();console.log(`Self-healed step ${stepIndex+1}: pushed start date to ${newStart.toISOString()} to maintain gap.`)}continue}console.log(`Step ${stepIndex+1} (schedule ${schedule.id}): Previous step complete. Proceeding.`);if(schedule.campaigns.current_step!==stepIndex+1){await supabaseAdmin.from("campaigns").update({current_step:stepIndex+1}).eq("id",schedule.campaign_id);schedule.campaigns.current_step=stepIndex+1}}else{if(schedule.campaigns.current_step!==1){await supabaseAdmin.from("campaigns").update({current_step:1}).eq("id",schedule.campaign_id);schedule.campaigns.current_step=1}}const minInterval=Math.max(5,schedule.interval_minutes||15);const{data:lastSent}=await supabaseAdmin.from("campaign_progress").select("sent_at").eq("campaign_id",schedule.campaign_id).eq("schedule_id",schedule.id).eq("status","sent").not("sent_at","is",null).order("sent_at",{ascending:false}).limit(1).maybeSingle();if(lastSent?.sent_at){const minutesSinceLast=(now.getTime()-new Date(lastSent.sent_at).getTime())/(1e3*60);if(minutesSinceLast<minInterval){console.log(`Skipping schedule ${schedule.id}: ${Math.round(minutesSinceLast)}min since last (need ${minInterval}min).`);continue}}const{data:rawScheduleAccounts}=await supabaseAdmin.from("schedule_email_accounts").select(`*, email_accounts!inner(*)`).eq("schedule_id",schedule.id);if(!rawScheduleAccounts||rawScheduleAccounts.length===0)continue;const scheduleAccounts=rawScheduleAccounts.filter(sa=>{const email=sa.email_accounts?.email?.toLowerCase()||"";if(email.includes("mrmedic")||email.includes("mrmedicevents.co.uk")){console.log(`Filtering out MrMedic email account: ${email}`);return false}return true});if(scheduleAccounts.length===0){console.log(`Schedule ${schedule.id}: No active non-MrMedic email accounts.`);continue}const{data:pendingLeads,error:pendingError}=await supabaseAdmin.rpc("get_pending_campaign_leads",{campaign_id_param:schedule.campaign_id,schedule_id_param:schedule.id}).limit(scheduleAccounts.length*50);if(pendingError){console.error("Error fetching pending leads",pendingError);continue}if(!pendingLeads||pendingLeads.length===0){console.log(`Schedule ${schedule.id}: No pending leads.`);continue}const leadEmails=pendingLeads.map(l=>l.email).filter(Boolean);let crossCampaignSentEmails=new Set;if(leadEmails.length>0){const{data:alreadySent}=await supabaseAdmin.from("campaign_progress").select("leads!inner(email)").neq("campaign_id",schedule.campaign_id).eq("status","sent").in("leads.email",leadEmails);if(alreadySent){for(const row of alreadySent){if(row.leads?.email)crossCampaignSentEmails.add(row.leads.email.toLowerCase())}}if(crossCampaignSentEmails.size>0){console.log(`[DEDUP] Filtering ${crossCampaignSentEmails.size} leads already targeted by other campaigns.`)}}console.log(`Schedule ${schedule.id}: Processing ${pendingLeads.length} leads across ${scheduleAccounts.length} accounts.`);let sentCount=0;let accountIndex=0;for(const lead of pendingLeads){if(!lead.email||lead.email.trim()===""||!lead.email.includes("@")){console.warn(`Skipping lead ${lead.id}: Invalid email "${lead.email}"`);await supabaseAdmin.from("campaign_progress").upsert({campaign_id:schedule.campaign_id,schedule_id:schedule.id,lead_id:lead.id,email_account_id:scheduleAccounts[0].email_accounts.id,status:"failed",updated_at:new Date().toISOString()},{onConflict:"campaign_id,schedule_id,lead_id"});continue}if(crossCampaignSentEmails.has(lead.email.toLowerCase())){console.log(`[DEDUP] Skipping lead ${lead.email}: Already targeted by another campaign.`);await supabaseAdmin.from("campaign_progress").upsert({campaign_id:schedule.campaign_id,schedule_id:schedule.id,lead_id:lead.id,email_account_id:scheduleAccounts[0].email_accounts.id,status:"failed",updated_at:new Date().toISOString()},{onConflict:"campaign_id,schedule_id,lead_id"});continue}if(["interested","replied","unsubscribed","bounced"].includes(lead.status)){console.log(`Skipping lead ${lead.email}: Status is ${lead.status}`);await supabaseAdmin.from("campaign_progress").upsert({campaign_id:schedule.campaign_id,schedule_id:schedule.id,lead_id:lead.id,email_account_id:scheduleAccounts[0].email_accounts.id,status:lead.status==="unsubscribed"?"unsubscribed":"replied",updated_at:new Date().toISOString()},{onConflict:"campaign_id,schedule_id,lead_id"});continue}let account=null;if(lead.assigned_email_account_id){const found=scheduleAccounts.find(a=>a.email_accounts.id===lead.assigned_email_account_id);if(found){account=found.email_accounts}else{const{data:directAcc}=await supabaseAdmin.from("email_accounts").select("*").eq("id",lead.assigned_email_account_id).single();if(directAcc){account=directAcc;console.log(`Lead ${lead.email} using consistently assigned account ${account.email} even if not explicitly in schedule.`)}}}if(!account){const accountEntry=scheduleAccounts[accountIndex%scheduleAccounts.length];account=accountEntry.email_accounts;accountIndex++;try{await supabaseAdmin.from("campaign_leads").update({assigned_email_account_id:account.id}).eq("campaign_id",schedule.campaign_id).eq("lead_id",lead.id)}catch(e){console.error("Failed to save assignment",e)}}let leadReply=null;let ourLastEmail=null;let originalSubject=null;try{const{data:ourSentEmails}=await supabaseAdmin.from("inbox_emails").select("subject, body_text, received_at").eq("campaign_id",schedule.campaign_id).eq("to",lead.email).eq("folder","sent").order("received_at",{ascending:false}).limit(1);if(ourSentEmails&&ourSentEmails.length>0){const lastSent2=ourSentEmails[0];ourLastEmail=lastSent2.body_text;originalSubject=lastSent2.subject;let cleanSubject=lastSent2.subject.replace(/^(Re|Fwd|Fw|Aw|Reply):\s*/i,"").trim();let safeSubject=cleanSubject.replace(/[%_]/g,"\\$&");const{data:possibleReplies}=await supabaseAdmin.from("inbox_emails").select("body_text, received_at").eq("folder","inbox").gte("received_at",lastSent2.received_at).or(`from.ilike.%${lead.email}%,subject.ilike.%${safeSubject}%`).order("received_at",{ascending:true});if(possibleReplies&&possibleReplies.length>0){leadReply=possibleReplies.map(r=>r.body_text).join("\n---\n")}}}catch(scanErr){console.error("Error scanning inbox",scanErr)}if(leadReply){console.log(`[Campaign ${schedule.campaign_id}] Lead ${lead.email} replied. Halting sequence.`);await supabaseAdmin.from("leads").update({status:"interested"}).eq("id",lead.id);await supabaseAdmin.from("campaign_progress").upsert({campaign_id:schedule.campaign_id,schedule_id:schedule.id,lead_id:lead.id,email_account_id:account.id,status:"replied",updated_at:new Date().toISOString()},{onConflict:"campaign_id,schedule_id,lead_id"});const{data:otherSchedules}=await supabaseAdmin.from("scheduled_emails").select("id").eq("campaign_id",schedule.campaign_id).neq("id",schedule.id);if(otherSchedules&&otherSchedules.length>0){for(const other of otherSchedules){await supabaseAdmin.from("campaign_progress").upsert({campaign_id:schedule.campaign_id,schedule_id:other.id,lead_id:lead.id,email_account_id:account.id,status:"replied",updated_at:new Date().toISOString()},{onConflict:"campaign_id,schedule_id,lead_id"})}}continue}let firstName=(lead.name||"").trim();let companyName=(lead.company||"").trim();let safeFirstName=firstName.split(" ")[0];const lowerName=firstName.toLowerCase();if(!firstName||lowerName==="the"||lowerName.startsWith("the ")||lowerName.startsWith("a ")||lowerName.startsWith("an ")||companyName&&lowerName===companyName.toLowerCase()){safeFirstName="there"}let bodyContent=lead.personalized_email;let subjectContent=lead.personalized_subject;const isStep1=stepIndex===0;const hasPlaceholders=schedule.templates.content.includes("{")||schedule.templates.content.includes("[")||(schedule.templates.subject.includes("{")||schedule.templates.subject.includes("["));if((isStep1||!bodyContent||!subjectContent||hasPlaceholders)&&lead.summary){try{const leadFirstName=safeFirstName;let templateBodyForAI=schedule.templates.content.replace(/\n*\{ender\}[\s\S]*$/i,"").replace(/\n*\{\{ender\}\}[\s\S]*$/i,"").replace(/\n*\[Sender Name\][\s\S]*$/i,"").replace(/\n*<company>[\s\S]*$/i,"").trim();const systemPrompt="You are a world-class B2B sales strategist personalizing cold outreach. Your goal is to rewrite the provided email template to be highly relevant to the specific lead based on their business summary.\n\nCRITICAL RULES:\n1. DO NOT return a template with placeholders like [Name] or {{company}}. Return the FINISHED email.\n2. Use the lead's first name: "+leadFirstName+'.\n3. Write a unique, personalized opening sentence based on the provided Lead Notes.\n4. Maintain the core offer and call to action from the Original Template.\n5. Tone: Professional, helpful, concise, and slightly informal (like a colleague).\n6. ABSOLUTELY DO NOT include any sign-off, closing, or signature in the body. No Best, Regards, Cheers, Thanks, Sincerely, or ANY name at the end. The system auto-appends the correct sender signature. Including one will cause a DUPLICATE and a WRONG NAME.\n7. Output ONLY valid JSON: { "subject": "Customized subject line", "body": "Finished email body without any sign-off or signature" }';const userPrompt='Original Template Subject: "'+schedule.templates.subject+'"\nOriginal Template Body: "'+templateBodyForAI+'"\nLead Name: '+leadFirstName+"\nLead Company: "+(lead.company||"their business")+'\nLead Notes: "'+(lead.summary||"")+'"\n\nInstructions: Customize the subject and body for this lead. Remove all placeholders. Ensure the transition from the personalized opening to the core message is seamless.';let aiResp;if(disableDeepseek){console.log("DeepSeek is disabled. Trying OpenRouter key cycling...");let success=false;for(let k=0;k<OPENROUTER_KEYS.length;k++){const apiKey=OPENROUTER_KEYS[k];try{const res=await fetch("https://openrouter.ai/api/v1/chat/completions",{method:"POST",headers:{"Content-Type":"application/json","Authorization":`Bearer ${apiKey}`,"HTTP-Referer":"https://github.com/Openclaw-Factory","X-Title":"ColdSpark"},body:JSON.stringify({model:"openrouter/owl-alpha",messages:[{role:"system",content:systemPrompt},{role:"user",content:userPrompt}],response_format:{type:"json_object"}})});if(res.ok){const data=await res.json();if(data&&!data.error&&data.choices?.[0]){aiResp={status:200,ok:true,json:async()=>data,text:async()=>JSON.stringify(data)};success=true;break}else{console.error(`OpenRouter Key ${k+1} API Error:`,data?.error?.message||JSON.stringify(data?.error))}}else{console.error(`OpenRouter Key ${k+1} Status Error:`,res.status,await res.text())}}catch(err){console.error(`OpenRouter Key ${k+1} Network/Timeout Error:`,err)}}if(!success){aiResp={status:402,ok:false,json:async()=>({error:"All OpenRouter keys failed"}),text:async()=>"All OpenRouter keys failed"}}}else{aiResp=await fetch(DEEPSEEK_BASE_URL+"/chat/completions",{method:"POST",headers:{"Content-Type":"application/json","Authorization":"Bearer "+DEEPSEEK_API_KEY},body:JSON.stringify({model:"deepseek-chat",messages:[{role:"system",content:systemPrompt},{role:"user",content:userPrompt}],response_format:{type:"json_object"}})})}if(aiResp.status===402||aiResp.status===429||!aiResp.ok){const errText=await aiResp.text();console.error(`DeepSeek API Error (status ${aiResp.status}):`,errText);const isInsufficientCredits=aiResp.status===402||errText.toLowerCase().includes("balance")||errText.toLowerCase().includes("credit")||errText.toLowerCase().includes("insufficient");if(isInsufficientCredits){console.log("Credit exhaustion detected! Pausing factory engine.");await supabaseAdmin.from("agent_memory").upsert({key_name:"factory_status",value:{status:"paused",reason:"insufficient_credits"}},{onConflict:"key_name"});await supabaseAdmin.from("debug_logs").insert({level:"error",message:"DeepSeek personalizer paused sequence engine due to insufficient AI credits",context:{status:aiResp.status,error:errText,campaign_id:schedule.campaign_id}});return new Response(JSON.stringify({success:false,error:"Engine paused due to insufficient AI credits"}),{status:402,headers:{"Content-Type":"application/json"}})}else{throw new Error(`DeepSeek API non-ok response (${aiResp.status}): ${errText}`)}}else{const aiData=await aiResp.json();if(aiData.choices&&aiData.choices[0]){const rawContent=aiData.choices[0].message.content.trim();try{const jsonStr=rawContent.replace(/\`\`\`json\n|\n\`\`\`/g,"").trim();const parsed=JSON.parse(jsonStr);bodyContent=parsed.body||parsed.Body||rawContent;subjectContent=parsed.subject||parsed.Subject||""}catch{bodyContent=rawContent}await supabaseAdmin.from("leads").update({personalized_email:bodyContent,personalized_subject:subjectContent}).eq("id",lead.id)}}}catch(err){console.error("AI Personalization Failed",err)}}if(!bodyContent)bodyContent=schedule.templates.content;if(!subjectContent)subjectContent=schedule.templates.subject;let senderFirstName="Sender";if(account.name)senderFirstName=account.name.split(" ")[0];else if(account.email){senderFirstName=account.email.split("@")[0];senderFirstName=senderFirstName.charAt(0).toUpperCase()+senderFirstName.slice(1)}const senderPhone=schedule.campaigns.contact_number||account.phone_number||"";const senderCompany=schedule.campaigns.company_name||account.company||"";const senderName=account.name||senderFirstName;const senderEmail=account.email;const enders=["Best,","Kind regards,","Regards,","Warm regards,","Cheers,"];const randomEnder=enders[Math.floor(Math.random()*enders.length)];let strippedBody=bodyContent;const signOffStrip=/\n*\s*(Best|Kind regards|Regards|Warm regards|Cheers|Thanks|Sincerely|Thank you|All the best|Take care),?\s*\n[\s\S]{0,200}$/i;strippedBody=strippedBody.replace(signOffStrip,"").trimEnd();strippedBody=strippedBody.replace(/\n*\s*\{\{?ender\}\}?[\s\S]*$/i,"").replace(/\n*\s*\[Sender Name\][\s\S]*$/i,"").trimEnd();const cleanSignature=account.signature?account.signature.trim():"";const isMrMedic=schedule.campaigns?.business_id==="0269fe06-4607-4c58-9263-12a3930a1dc3";const isRelay=schedule.campaigns?.business_id==="102a3bca-7b0a-4cee-bd33-fefd7b4450b4";const campaignName=(schedule.campaigns?.name||"").toLowerCase();let isWebDev=false;let isAI=false;let isAutomation=false;if(isRelay){if(campaignName.includes("web dev")||campaignName.includes("development")||campaignName.includes("website")){isWebDev=true}else if(campaignName.includes("ai")||campaignName.includes("artificial intelligence")||campaignName.includes("machine learning")){isAI=true}else if(campaignName.includes("automation")||campaignName.includes("workflow")){isAutomation=true}}const genericOptOutPhrases=["P.S. If you're not the right person for this or prefer I don't reach out again, just let me know.","If you'd prefer I stop emailing you, just reply and let me know.","Not interested? Just reply and I'll update my notes."];const mrmedicOptOutPhrases=["P.S. If event medical cover isn't on your radar right now, just let me know and I won't follow up.","If you'd rather not receive these updates, just drop me a quick reply.","Not the right time? Just let me know and I'll update my records."];const webDevOptOutPhrases=["P.S. If updating your web presence isn't on your roadmap right now, just let me know.","If you're already set with a great development team, just drop a quick reply so I don't bug you again.","Not the right time to talk websites? Just let me know and I'll step back."];const aiOptOutPhrases=["P.S. If AI isn't a priority for your operations right now, just let me know and I won't follow up.","If you'd prefer not to hear more about AI integration, just reply and I'll update my notes.","Not the right time for AI? Just drop me a quick reply."];const automationOptOutPhrases=["P.S. If automating your workflows isn't on the agenda right now, just let me know.","If your current processes are running perfectly, just drop a quick reply so I know not to reach out again.","Not focused on automation at the moment? Just let me know and I'll update my records."];let phrasesToUse=genericOptOutPhrases;if(isMrMedic){phrasesToUse=mrmedicOptOutPhrases}else if(isRelay){if(isWebDev)phrasesToUse=webDevOptOutPhrases;else if(isAI)phrasesToUse=aiOptOutPhrases;else if(isAutomation)phrasesToUse=automationOptOutPhrases;else phrasesToUse=genericOptOutPhrases}const randomOptOut=phrasesToUse[Math.floor(Math.random()*phrasesToUse.length)];let personalContent;if(cleanSignature){personalContent=`${strippedBody}

${cleanSignature}

${randomOptOut}`}else{const senderFullName=account.name||senderFirstName;personalContent=`${strippedBody}

${randomEnder}
${senderFullName}
${senderCompany}

${randomOptOut}`.trimEnd()}const replacements=[{pattern:/{{first_name}}|{first_name}|{firstName}|\[First Name\]/gi,val:safeFirstName},{pattern:/{{name}}|{name}|\[Name\]/gi,val:lead.name||safeFirstName},{pattern:/{{company}}|{company}|{companyName}|\[Company\]/gi,val:companyName||"your business"},{pattern:/{{industry}}|{industry}/gi,val:lead.industry||"industry"},{pattern:/{{location}}|{location}/gi,val:lead.location||""},{pattern:/{{sender_name}}|{sender_name}|\[Sender Name\]/gi,val:senderName},{pattern:/{{sender_email}}|{sender_email}|<primaryemail>|\[Email\]/gi,val:senderEmail},{pattern:/{{sender_phone}}|{sender_phone}|<contactnumber>|\[Phone\]/gi,val:senderPhone},{pattern:/{{sender_company}}|{sender_company}|<company>/gi,val:senderCompany},{pattern:/{{ender}}|{ender}/gi,val:randomEnder}];const signatureTemplate=schedule.campaigns?.businesses?.signature_template||"";let fullContent=personalContent;if(signatureTemplate){fullContent+="\n\n"+signatureTemplate}let finalBody=fullContent;let finalSubject=subjectContent;replacements.forEach(r=>{finalBody=finalBody.replace(r.pattern,r.val);finalSubject=finalSubject.replace(r.pattern,r.val)});const unsubscribeLink=`https://relay-mailer.com/api/unsubscribe?leadId=${lead.id}&campaignId=${schedule.campaign_id}`;finalBody+=`

---
<span style="font-size: 10px; color: #999;">To opt out of future emails, please <a href="${unsubscribeLink}" style="color: #999; text-decoration: underline;">click here</a>.</span>`;const placeholderRegex=/{{.*?}}|{.*?}|\[.*?\]/g;const unreplaced=(finalBody.match(placeholderRegex)||[]).filter(m=>!m.match(/^\[\s*\]$/));const subjectUnreplaced=finalSubject.match(placeholderRegex)||[];if(unreplaced.length>0||subjectUnreplaced.length>0){console.warn("Found unreplaced placeholders, marking as failed:",[...unreplaced,...subjectUnreplaced]);await supabaseAdmin.from("campaign_progress").upsert({campaign_id:schedule.campaign_id,schedule_id:schedule.id,lead_id:lead.id,email_account_id:account.id,status:"failed",updated_at:new Date().toISOString()},{onConflict:"campaign_id,schedule_id,lead_id"});continue}const{data:decrypted}=await supabaseAdmin.rpc("decrypt_password",{encrypted_password:account.encrypted_password});if(!decrypted){console.error("Failed to decrypt password for account",account.email);continue}const senderIdentifier=account.email.toLowerCase();if(senderIdentifier){const{data:canSend}=await supabaseAdmin.rpc("increment_domain_email_count",{p_domain:senderIdentifier,p_max_limit:50});if(!canSend){console.log(`Account limit reached for ${senderIdentifier}.`);continue}}try{const transporter=nodemailer.createTransport({host:account.smtp_host,port:account.smtp_port,secure:account.smtp_port===465,auth:{user:account.email,pass:decrypted}});await transporter.sendMail({from:account.name?'"'+account.name+'" <'+account.email+">":account.email,to:lead.email,subject:finalSubject,html:finalBody.replace(/\n/g,"<br/>"),text:finalBody});await supabaseAdmin.from("campaign_progress").upsert({campaign_id:schedule.campaign_id,schedule_id:schedule.id,lead_id:lead.id,email_account_id:account.id,status:"sent",sent_at:new Date().toISOString(),updated_at:new Date().toISOString()},{onConflict:"campaign_id,schedule_id,lead_id"});const{data:currentStats}=await supabaseAdmin.from("scheduled_emails").select("sent_emails").eq("id",schedule.id).single();if(currentStats){await supabaseAdmin.from("scheduled_emails").update({sent_emails:(currentStats.sent_emails||0)+1}).eq("id",schedule.id)}const{error:inboxError}=await supabaseAdmin.from("inbox_emails").insert({email_account_id:account.id,folder:"sent",uid:Math.floor(Math.random()*1e9),from:account.email,to:lead.email,subject:finalSubject,body_text:finalBody,body_html:finalBody.replace(/\n/g,"<br/>"),snippet:finalBody.substring(0,100),received_at:new Date().toISOString(),is_read:true,campaign_id:schedule.campaign_id,sequence_step:schedule.templates.name});if(inboxError){console.error("\u274C Inbox Insert Failed:",inboxError.message);await supabaseAdmin.from("debug_logs").insert({level:"error",message:`Failed to insert sent email to inbox: ${inboxError.message}`,context:{schedule_id:schedule.id,lead_id:lead.id}})}else{console.log("\u2705 Sent email persisted to inbox.")}sentCount++;results.push({email:lead.email,status:"sent",from:account.email})}catch(sendErr){console.error("\u274C Send Failed:",sendErr?.message||sendErr);await supabaseAdmin.from("campaign_progress").upsert({campaign_id:schedule.campaign_id,schedule_id:schedule.id,lead_id:lead.id,email_account_id:account.id,status:"failed",updated_at:new Date().toISOString()},{onConflict:"campaign_id,schedule_id,lead_id"})}}if(pendingLeads&&pendingLeads.length>0){const nextSend=new Date(now.getTime()+(schedule.interval_minutes||5)*6e4);await supabaseAdmin.from("scheduled_emails").update({scheduled_for:nextSend.toISOString()}).eq("id",schedule.id)}}return new Response(JSON.stringify({success:true,processed:results}),{headers:{"Content-Type":"application/json"}})}catch(err){return new Response(String(err?.message??err),{status:500})}});
